# Supabase migrations

Place timestamped SQL migrations in this directory.

The initial migration creates the staff profile and appointment tables and adds conservative RLS policies that keep the scaffold fail-closed until a full authorization model is implemented.
